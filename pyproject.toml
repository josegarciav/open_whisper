[project]
name = "open-whisper"
version = "0.3.0"
description = "Local Speech-to-Text — no cloud, no limits"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "faster-whisper>=1.2.1",
    "flask>=3.0.0",
    "sounddevice>=0.4.6",
    "numpy>=1.24.0",
    "soundfile>=0.12.0",
]
